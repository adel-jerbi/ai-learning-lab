# LLM — local (OpenAI-compatible via Ollama)
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.2

# LLM — cloud (fill in .env only; never commit real keys)
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Postgres + pgvector
DATABASE_URL=postgresql://ai:ai@localhost:5432/ai_learning